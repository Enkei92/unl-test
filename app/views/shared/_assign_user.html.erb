<%= form_for(task.assignments.build(task_id: task.id)) do |f| %>
  <%= f.collection_select :user_id, User.all, :id, :email %>
  <%= f.hidden_field :task_id %>

  <%= f.submit 'Assign user', class: 'btn primary' %>
<% end %>  