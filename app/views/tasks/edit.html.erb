<h3> Edit task: </h3>
<%= form_for @task do |f| %>

<% if @task.errors.any? %>
  <h2>Errors: </h2>
  <ul>
    <% @task.errors.full_messages.each do |msg| %>
      <li> <%= msg %> </li>
    <% end %>
  </ul>
<% end %>

  <div class="form-group">
    <%= f.label :title %>
    <%= f.text_field :title, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :set_status %><br />
    <%= f.select :status, Task.statuses.keys %>
  </div>
  <div class="field">
    <%= f.label :assign_user %><br />
    <%= f.association :user_id, User.all, :id, :email %>
  </div>
  <div class="form-group">
    <%= f.submit "Save", class: 'btn btn-primary' %>
    <%= link_to "Cancel", :back, class: 'btn btn-outline-primary' %>
  </div>
<% end %>